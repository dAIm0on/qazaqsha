(function(){
 'use strict';
 const cfg=window.TRAINER_CONFIG,core=window.TrainerCore,progress=window.ProgressStore,catalog=window.CURRICULUM;
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 function create(api){
   let wordQuery='',wordFilter='all';
   function render(view){
     const root=document.getElementById(view+'-content');if(!root)return;
     const state=api.state(),cards=api.questions().filter(api.eligible),rows=Array.from(new Set(cards.flatMap(q=>window.Knowledge.bindings(q).map(window.Knowledge.key)))).map(id=>state.skills[id]).filter(Boolean);
     const due=window.Knowledge.choose(cards.filter(q=>core.isDue(state.records[q.id])),state,Infinity).length,newCount=cards.filter(q=>!state.records[q.id]?.seen).length;
     const lesson=window.LEARNING.lessons.find(l=>l.id===state.learning.lessonId)||window.LEARNING.lessons[0];
     if(view==='today'){
       const retention=window.LearningSupport.retention(state);
       const weakWords=catalog.words.filter(w=>catalog.wordStats(w,state).weak),weakRules=catalog.rules.filter(r=>cards.some(q=>q.ruleIds?.includes(r.id)&&state.records[q.id]?.needsReview));
       const ms=progress.memoryStats(state);
       const weak=window.LearningSupport.weakSpots?window.LearningSupport.weakSpots(state,api.questions()):[];
       const chunkN=(window.MemoryPolicy?api.questions().filter(q=>window.MemoryPolicy.isChunk(q)&&api.eligible(q)):[]).length;
       const resume=api.hasSession();
       const mainAction=resume?'resume':'learn';
       const mainTitle=resume?'Продолжить текущую карточку':('Продолжить урок '+lesson.courseLesson);
       const mainHint=resume?'Продолжим с той же карточки.':esc(lesson.title);
       const dueHint=due?'карточек в очереди повторения':'На сегодня всё повторено';
       const pauseN=cards.filter(q=>core.pauseReady(state.records[q.id])).length;
       const recallLine=retention.total?retention.correct+' из '+retention.total+' проверок после паузы — самостоятельно.':'';
       const tip=(id,text)=>`<details class="help-line"><summary aria-label="Что это значит">?</summary><p id="${id}">${text}</p></details>`;
       const pairN=progress.pairs(state).length;
       const weakOpen=weak.length||weakWords.length||weakRules.length||(window.AiTutor&&window.AiTutor.topWeak().length);
       const curCourse=lesson.courseLesson||'';
       const courseTitles={'1-1':'Звуки и первые слова','1-2':'Окончания и числа','1-3':'Числа, количество и новые слова','2-1':'мен, сен, сіз; емес; ба/бе','2-2':'біз, сендер, сіздер и прилагательные','2-3':'ол / олар, вопрос, порядковые'};
       const lessonBtn=id=>`<button type="button" class="today-lesson${curCourse===id?' current':''}" data-action="course:${id}"><span class="today-lesson-id">${id.replace('-','–')}</span><span class="today-lesson-title">${courseTitles[id]}</span></button>`;
       const recallCards=cards.filter(q=>q.kind==='fields'&&(q.fields||[]).some(f=>f.kind!=='select'));
       const remembered=recallCards.filter(q=>['REMEMBERED','MASTERED'].includes(state.records[q.id]?.mastery_level)).length;
       root.innerHTML=`<div class="today-hero"><p>Сначала текущий урок, потом то, что пора вспомнить, потом домашка.</p><article class="today-hero-card"><p class="eyebrow">Главное сейчас</p><button type="button" class="today-main" data-action="${mainAction}"><span>${mainTitle}</span></button><p class="small">${mainHint}</p><button type="button" class="today-path-quiet text-button" data-action="path">Прохождение</button></article></div>
       <div class="today-tiles"><button type="button" class="today-option" data-action="review"><span>Пора вспомнить</span><strong>${due}</strong><small>${dueHint}</small></button><button type="button" class="today-option" data-action="homework"><span>Домашка</span><small>Задания урока</small></button>${pauseN?`<button type="button" class="today-option" data-action="pause-prep"><span>Готовится к паузе</span><strong>${pauseN}</strong><small>Интервал уже длинный</small></button>`:''}</div>
       <div class="today-actions"><button type="button" class="today-option" data-action="new"><span>Новые</span><strong>${Math.min(cfg.session.newLimit,newCount)}</strong><small>Рекомендовано на один подход</small></button><button type="button" class="today-option today-rules-quiet" data-view="rules"><span>Правила</span><small>Справочник</small></button>${chunkN?'<button type="button" class="today-option" data-action="chunks"><span>Приветствия и прощания</span><small>Готовые фразы</small></button>':''}</div>
       <div class="today-collage" aria-hidden="true"><div class="today-photo p1"></div><div class="today-photo p2"></div><div class="today-photo p3"></div><div class="today-photo p4"></div></div>
       <div class="today-lessons" aria-label="Уроки курса">${lessonBtn('1-1')}${lessonBtn('1-2')}${lessonBtn('1-3')}${lessonBtn('2-1')}${lessonBtn('2-2')}${lessonBtn('2-3')}</div>
       <details class="panel compact-panel"><summary>Память</summary><div class="panel-head">${tip('memory-help','Показывает, насколько материал вспоминается после паузы, а не процент знания языка.')}</div><p>Помню после паузы: <strong>${remembered} / ${recallCards.length}</strong></p>${recallLine?'<p>'+recallLine+'</p>':''}${ms.firstTry==null?'':'<p>Вспомнила самостоятельно: '+ms.firstTry+'%</p>'}${ms.peekRate==null?'':'<p>С подсказкой: '+ms.peekRate+'%</p>'}${ms.stop?'<p class="small">Если три дня подряд больше половины домашки открывается с подсказкой — уменьши пачку, не добавляй новый тип.</p>':''}</details>
       <details class="panel compact-panel"${weakOpen?' open':''}><summary>Слабые места</summary>${weak.length?weak.map(w=>`<div class="confusion-row"><div><strong>${esc(window.Homework&&window.Homework.weakLabel?window.Homework.weakLabel(w.key):w.key)}</strong><p class="small">${w.count} за 14 дней. ${esc(w.expected)} → ${esc(w.actual)}</p></div><button type="button" class="secondary-button" data-action="weak:${esc(w.cardId)}">Разобрать</button></div>`).join(''):'<p>Устойчивых слабых мест пока нет.</p>'}${weakWords.length?'<p>'+weakWords.slice(0,5).map(w=>esc(w.kazakh)).join(' · ')+'</p>':''}${weakRules.length?'<p class="small">Правила: '+weakRules.map(r=>esc(r.title)).join(', ')+'</p>':''}${window.AiTutor&&window.AiTutor.topWeak().length?'<p class="small">'+window.AiTutor.topWeak().map(w=>esc(window.AiTutor.label(w.error_code))+' · '+w.count_recent).join('<br>')+'</p><button type="button" class="text-button" data-action="ai-summary">Краткий разбор сессии</button>':''}${pairN?`<button type="button" class="text-button" data-action="confusions">Часто путаю: ${pairN} →</button>`:''}</details>
       <details class="panel compact-panel"><summary>Навыки</summary><div class="useful-stats"><div><strong>${rows.filter(r=>['LEARNING','FAMILIAR'].includes(r.mastery_level)).length}</strong><span>Изучается / знакомо</span></div><div><strong>${rows.filter(r=>r.mastery_level==='REMEMBERED').length}</strong><span>Помню после паузы</span></div><div><strong>${rows.filter(r=>r.mastery_level==='MASTERED').length}</strong><span>Устойчиво вспоминаю</span></div></div><p class="small">Произношение голосом приложение не проверяет. Тренируем буквы, гармонию и набор.</p></details>
       <details class="panel compact-panel"><summary>Мой словарь и материалы</summary><div class="utility-links"><button type="button" class="secondary-button" data-action="vocabulary">Мой словарь</button><button type="button" class="text-button" data-action="materials">Материалы курса</button></div></details>${backupMarkup()}`;
     }else if(view==='review'){
       const pairs=progress.pairs(state),remedies=window.LearningSupport.suggestions(state,api.questions());
       const errorCounts={};for(const e of state.errors)errorCounts[e.error_type]=(errorCounts[e.error_type]||0)+1;
       const times=state.events.filter(e=>e.type==='answer'&&e.correct&&!e.hinted&&e.response_time_ms!=null);
       const rt=types=>{const values=times.filter(e=>e.skills?.some(s=>types.includes(s.skill_type))).slice(-30).map(e=>e.response_time_ms).sort((a,b)=>a-b);return values.length?{n:values.length,median:values[Math.floor(values.length/2)]/1000}:null;};
       const digitTime=rt(['digit_to_word']),readTime=rt(['word_to_digit']);
       root.innerHTML=`${due?`<article class="review-due"><p class="eyebrow">Очередь на сегодня</p><h2>Пора вспомнить</h2><p>Только то, что пора вернуть по памяти.</p><button type="button" class="primary-button" data-action="review">Пора повторить · ${due}</button></article>`:'<div class="empty-state"><img class="empty-graphic" src="assets/graphics/g02-empty-review.svg" alt=""><h2>На сегодня всё повторено</h2><p>Можно открыть знакомые карточки или вернуться к уроку.</p></div>'}<div class="review-actions">${due?'':'<button type="button" class="primary-button" data-action="review">Открыть знакомые</button>'}<button type="button" class="secondary-button" data-action="mistakes">Мои ошибки</button><button type="button" class="secondary-button" data-action="smart">Короткий подход</button></div>${remedies.length?'<div class="panel"><h2>Помощь с трудным шагом</h2>'+remedies.map(h=>'<p><button type="button" class="secondary-button" data-action="remedy:'+h.type+'">'+esc(h.title)+'</button></p>').join('')+'</div>':''}<details class="panel"><summary>Числа: лестница</summary><p>Иди по порядку. Сначала слова, потом пары 6/60, потом сборка. В сотнях и тысячах первое время 5 встречается вместе с 50, 8 — с 80.</p><div class="pair-strip pair-strip-compact"><span lang="kk">алты / алпыс · 6 / 60</span><span lang="kk">жеті / жетпіс · 7 / 70</span><span lang="kk">сегіз / сексен · 8 / 80</span><span lang="kk">тоғыз / тоқсан · 9 / 90</span></div><div class="review-actions"><button type="button" class="secondary-button" data-action="number:tens">Десятки</button><button type="button" class="secondary-button" data-action="number:contrast">6↔60, 8↔80</button><button type="button" class="secondary-button" data-action="number:mix2">5 и 52 · 8 и 89</button><button type="button" class="secondary-button" data-action="number:echo2">55 · 88 · 66</button><button type="button" class="secondary-button" data-action="number:composition">11–99 любые</button><button type="button" class="secondary-button" data-action="number:echo3">Сотни 550 · 880</button><button type="button" class="secondary-button" data-action="number:hundreds">Сотни любые</button><button type="button" class="secondary-button" data-action="number:echo4">Тысячи 1550 · 8080</button><button type="button" class="secondary-button" data-action="number:thousands">Тысячи любые</button></div></details><div class="panel"><h2>Путаю</h2><p class="small">Пара появляется после ${cfg.confusion.minCount} повторений одной ошибки. Неизвестный вариант не считается новым правильным словом.</p>${pairs.length?pairs.map(([,p],i)=>`<div class="confusion-row"><div><strong>${esc(p.expected_answer)} ↔ ${esc(p.wrong_answer_given)}</strong><p class="small">Повторилось: ${p.confusion_count}. ${p.known_alternative?'Оба варианта встречаются в базе.':'Второй вариант не подтверждён: возможна опечатка. Тренируем исходный ответ.'}</p></div><button class="secondary-button" type="button" data-pair="${i}">Различить</button></div>`).join(''):'<p>Повторяющихся подмен пока нет. Они определяются по твоим ответам, а не по заранее заданному списку.</p>'}</div><details class="panel"><summary>Частые ошибки и скорость</summary>${Object.entries(errorCounts).sort((a,b)=>b[1]-a[1]).map(([type,n])=>'<p>'+esc(window.ErrorDiagnostics.labels[type]||type)+': '+n+'</p>').join('')||'<p>Ошибок пока нет.</p>'}${[['Цифры → слово',digitTime],['Слово → цифры',readTime]].filter(([,t])=>t).map(([label,t])=>'<p>'+label+': медиана '+t.median.toFixed(1)+' с, ответов: '+t.n+'.</p>').join('')}<p class="small">Последние 30 самостоятельных правильных ответов каждого типа. Это время ввода целого ответа, включая набор на клавиатуре. Оно не меняет расписание.</p></details><details class="panel"><summary>Все режимы</summary><div class="review-actions"><button type="button" class="secondary-button" data-action="ordered">По материалам</button><button type="button" class="secondary-button" data-action="shuffle">Вперемешку</button></div></details>`;
       root.querySelectorAll('[data-pair]').forEach(b=>b.onclick=()=>api.contrast(pairs[Number(b.dataset.pair)][1]));
     }else if(view==='vocabulary'){
       root.innerHTML=`<div class="dictionary-controls"><label for="word-search">Найти слово</label><input id="word-search" type="search" value="${esc(wordQuery)}" placeholder="Казахское слово или перевод"><label for="word-filter">Показывать</label><select id="word-filter"><option value="all">Все слова</option><option value="target">Обязательные</option><option value="context">Для контекста</option><option value="known">Знакомые из прошлых уроков</option><option value="weak">Слабые</option></select></div><div id="dictionary-list"></div>${backupMarkup()}`;
       const draw=()=>{
         const query=core.normalize(wordQuery),list=catalog.words.filter(w=>{const s=catalog.wordStats(w,state);return (!query||core.normalize(w.kazakh+' '+w.translation.join(' ')).includes(query))&&(wordFilter==='all'||(wordFilter==='weak'?s.weak:wordFilter==='known'?s.vocabulary_status==='KNOWN':s.target_or_context===wordFilter));});
         root.querySelector('#dictionary-list').innerHTML=list.length?list.map(w=>{
           const s=catalog.wordStats(w,state);return `<article class="panel dictionary-card"><div class="lesson-top"><h2 lang="kk">${esc(w.kazakh)}</h2><span class="word-type">${s.vocabulary_status==='KNOWN'?'Знакомое · остаётся в повторении':s.target_or_context==='target'?'Обязательное':'Для контекста'}</span></div><p>${esc(w.translation.join(' / '))}</p><p class="small">Впервые: ${w.lesson_first_seen} · показов в заданиях: ${s.times_seen} · ${cfg.labels[s.mastery]}</p>${window.Knowledge.wordSkills(w,state).map(skill=>'<p class="small">'+esc(skill.label)+': '+cfg.labels[skill.record?.mastery_level||'NEW']+'</p>').join('')}${w.examples.length?`<details><summary>Примеры</summary>${w.examples.map(ex=>`<p lang="kk">${esc(ex.kazakh)}<br><span class="small" lang="ru">${esc(ex.translation)}</span></p>`).join('')}</details>`:''}<details class="personal-cue"><summary>Моя ассоциация</summary><label>Подсказка к ${esc(w.kazakh)}<textarea data-association="${esc(w.id)}" maxlength="1200" rows="2">${esc(s.association)}</textarea></label></details>${s.target_or_context==='context'?(window.MemoryPolicy&&!window.MemoryPolicy.canAddIncidental(state)?'<p class="small">Недельный потолок контекстных слов (8) уже набран. Слово остаётся в глоссарии.</p>':`<button type="button" class="text-button" data-promote="${esc(w.id)}">Добавить в обязательное повторение</button>`):''}</article>`;}).join(''):'<div class="empty-state"><img class="empty-graphic" src="assets/graphics/g03-empty-search.svg" alt=""><p>Пока не нашли. Попробуй короче или выбери все разделы.</p></div>';
         root.querySelectorAll('[data-association]').forEach(el=>el.oninput=()=>api.setAssociation(el.dataset.association,el.value));
         root.querySelectorAll('[data-promote]').forEach(b=>b.onclick=()=>{api.promote(b.dataset.promote);render('vocabulary');});
       };
       root.querySelector('#word-filter').value=wordFilter;root.querySelector('#word-search').oninput=e=>{wordQuery=e.target.value;draw();};root.querySelector('#word-filter').onchange=e=>{wordFilter=e.target.value;draw();};draw();
     }
     root.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>api.action(b.dataset.action));
     root.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>{
       const nav=document.querySelector('.bottom-nav [data-view="'+b.dataset.view+'"], .topnav [data-view="'+b.dataset.view+'"]');
       if(nav)nav.click();
     }));
     bindBackup(root);
     const slot=root.querySelector('.install-slot'),inst=document.getElementById('installation-panel');
     if(slot&&inst&&inst.parentElement!==slot)slot.appendChild(inst);
   }
   function backupMarkup(){return `<details class="panel data-settings" id="data-settings"><summary>Настройки и перенос данных</summary><p class="small">Прогресс хранится в этом браузере. В другом браузере ответов не будет, пока не импортируешь файл.</p><button type="button" class="secondary-button" data-export>Экспорт прогресса</button><label class="import-label">Импортировать копию<input type="file" data-import accept="application/json,.json"></label><div class="import-preview" role="status"></div><p class="small">Перед импортом текущая копия сохраняется здесь. Счётчики не складываются, чтобы не удвоить ответы.</p><button type="button" class="text-button" data-restore>Скачать копию до последнего импорта</button><div class="install-slot"></div><details class="danger-actions"><summary>Опасные действия</summary><p class="small">Ответы, ошибки, заметки и ассоциации будут очищены.</p><button type="button" class="text-button" data-action="reset">Сбросить прогресс</button></details></details>`;}
   function bindBackup(root){
     const exp=root.querySelector('[data-export]');if(!exp)return;exp.onclick=api.export;root.querySelector('[data-restore]').onclick=api.restoreBackup;
     root.querySelector('[data-import]').onchange=async e=>{
       const file=e.target.files[0],preview=root.querySelector('.import-preview');if(!file)return;
       try{
         if(file.size>cfg.storage.maxImportBytes)throw Error('Файл слишком большой: максимум 20 МБ.');
         const pending=progress.validate(await file.text(),new Set(api.questions().map(q=>q.id))),s=pending.summary;
         preview.innerHTML=`<p>Карточек: ${s.cards}; ассоциаций: ${s.associations}; событий: ${s.events}. ${esc(pending.warning)}</p><label>Как перенести<select class="import-mode"><option value="merge">Объединить: более свежие записи</option><option value="replace">Заменить текущий прогресс</option></select></label><button type="button" class="primary-button confirm-import">Импортировать</button>`;
         preview.querySelector('.confirm-import').onclick=()=>{const mode=preview.querySelector('.import-mode').value;if(!window.confirm(mode==='replace'?'Заменить текущий прогресс этой копией? Прежние данные сохранятся в резервной копии браузера.':'Объединить данные? Для каждой карточки останется более свежая запись.'))return;try{api.import(pending.state,mode);}catch(error){preview.textContent=error.message;}};
       }catch(error){preview.textContent=error.message;}
     };
   }
   return {render};
 }
 window.DashboardUI={create};
})();
