# Архив степи · 16.09.2026

Старые картинки и шрифты, которые больше не показываются. Эталон вида их не использует.

Картинки: горизонт, пустой повтор, пустой поиск, завершение. Цвет линии `#41573F`.
Шрифты: Golos Text и Noto Serif. Их сменили IBM Plex. Казахский остаётся Segoe UI и из файлов не берётся.

Не подключать обратно в `sw.js` и не вставлять в экраны.
